Play with creating a multinode rails stack. This should consider
 * templated configuration (maybe etcd and confd)
 * cron jobs that run both on only once and all nodes
 * service discovery of database
